# Worklog — suzanneravenall

Session trail for the EA morning brief. Append one entry per work chunk:
`YYYY-MM-DD ~HH:MM | area | who | what happened | open: what's left`
Newest at the bottom. Never rewrite old lines.

2026-08-19 ~10:30 | session | EA | Dedicated Suzanne session kicked off from the Brain (/work suzanne). Target: VPS deploy of aa88f18+92dfee7, migration+seed+env steps, one PayFast sandbox checkout, FIX 13 click-test sweep, then point-by-point reply draft | open: everything - session starting
2026-08-19 ~10:30 | deploy+env | Claude | VPS already at 92dfee7 via 17-Aug CI run (verified bundle contains new coach CTA copy); switched box from feature/redesign-pass-1 to main; added EMAIL_UNSUBSCRIBE_SECRET (generated on-box) + COMPANY_PHYSICAL_ADDRESS to infra/.env, COMPANY_VAT_NUMBER confirmed unset (plain-invoice mode); new compose mapping fbaa041 pushed+pulled; web container recreated healthy, env verified in-container | open: supabase migration, inventory scripts, meilisearch, sentry DSNs (blocked - no source), VAT check
2026-08-19 ~10:35 | supabase | Claude | email_unsubscribes migration applied to suzanne DB on aimate-db1 (backup first: ~/backups/suzanne-pre-unsub-20260819.dump, 263K); table + RLS verified, service_role grants confirmed, PostgREST schema cache reloaded + suzanne-rest restarted | open: inventory scripts, meilisearch, VAT check
2026-08-19 ~11:00 | inventory KI030 | Claude | SURPRISE root cause: ALL product_variant_inventory_item link rows were soft-deleted on VPS 2026-06-09 (cause unknown) - 7 group-session Live variants had no inventory link so seeder skipped them. Fix: new repo script restore-group-session-inventory-links.ts (77ef5f9, code-reviewed, idempotency fix applied) creates fresh items + links via admin API; ran dry then live (7 restored), re-ran seed-group-session-inventory.ts (7x stocked_quantity=12), store API returns inventory_quantity:12. fix-variant-inventory-flags dry run showed 0 to change (already applied earlier). Attraction Frequency is recorded-only (no Live seat, by design). Medusa DB backup: /root/backups/medusa-pre-inventory-20260819.dump | open: meilisearch, VAT done (no tax regions/rates exist - nothing to remove)
2026-08-19 ~11:10 | meilisearch KI020 | Claude | Seeded on VPS via one-shot admin JWT (98 products + 8 topics); /api/search?q=confidence returns 200 with results. KNOWN_ISSUES updated: KI030 resolved, KI020 resolved (durable MEDUSA_API_TOKEN for n8n reindex still recommended), KI007 VAT side verified clean (zero tax_region/tax_rate rows on VPS - nothing to remove) | open: Sentry DSNs blocked (placeholders on VPS, no source in api-catalog, Vaultwarden needs Johan), PayFast sandbox blocked (creds are placeholders, bw CLI not on this PC)
2026-08-19 ~12:15 | FIX 13 sweep | Claude | All 8 Diagnostic Hub quizzes verified end-to-end server-side (gate renders, subscribe writes row+token, tokened page renders, complete returns correct resultKey; 58/58 quiz unit tests pass locally). Full visual click-through impossible this session (browser pane never composites - framer-motion flows stall; recommend one human spot-check). Sitewide: all 60 sitemap+nav routes 200; search OK; Cal discovery-call booking page OK; cart add OK in ZAR incl. group-session Live seat. FINDINGS: (1) quiz subscribe 500s = Resend domain unverified (deferred to cutover BY DESIGN but the Diagnostic Hub visibly fails for reviewers NOW); (2) NEW KI035 contact form silently drops messages while returning success (same Resend root cause); (3) NEW KI034 USD/International region: 123/207 variants have no USD price - add-to-cart 500 for non-ZA visitors; (4) FIX 12 confirmed open: both pattern-coach trial CTAs open old Pocket Coach site; (5) Sentry DSN placeholder baked into client bundle logs console error on every page (KI001) | open: reply draft
2026-08-19 ~12:25 | wrap-up | Claude | Reply draft written to C:\Johan Brain\drafts\2026-08-19-suzanne-review-reply.md (DRAFT ONLY, Johan approves) - separates re-look items (contrast, nav highlight, magazines, coach copy, 8 quizzes live) from open ones (brightness pass, group photo, FIX 12 trial-button destination, chat scoping, media article decisions) + caveat that quiz emails/contact form activate at Resend verification. BUILD_STATUS + KNOWN_ISSUES updated (KI030/KI020 resolved; KI034 USD pricing + KI035 contact silent-drop opened). BLOCKED ON JOHAN: (1) PayFast sandbox checkout - creds in Vaultwarden, bw CLI not on this PC; (2) Sentry DSNs - no accessible source; (3) Resend domain verify-now-vs-cutover decision; (4) human spot-check of one quiz on a real screen (headless pane cannot run framer-motion flows)
2026-08-20 ~08:25 | design | Claude | Colour diagnosis vs tonyrobbins.com done (homepage = worst offender: 8/15 dark, 6 flat; audit + screenshot comparison); homepage brightness pass implemented on feature/homepage-brightness-pass (11 files) | open: build check, review, before/after screenshots
2026-08-20 ~08:35 | design | Claude | Homepage brightness pass committed a473c6c on feature/homepage-brightness-pass (pushed, not merged/deployed): press band + programs + focus areas + spotlight flipped light, LeadMagnet/FinalCTA/1-on-1 card got photos behind navy, contrast raised sitewide on homepage; build green, 159/159 tests, review fixes applied | open: Johan reviews before/after, deploy branch to VPS for Suzanne, sitewide rollout decision (/shop, /about next-worst)
2026-08-20 ~09:00 | deploy | Claude | feature/homepage-brightness-pass (0a4956a) deployed to VPS on Johan's instruction: branch checked out, infra-web rebuilt (~6 min) and recreated; verified live (200, light press-band markup in HTML, real cohort card '12 spots left' renders, new CTA photos referenced) | open: Suzanne review, then sitewide rollout decision (/shop, /about next)
2026-08-20 ~09:20 | design | Claude | Blue-on-image fixes deployed (c5b9780): hero blue text -> white + accent underline, program card navy tint -> neutral scrim + white eyebrows/badge, focus/quote/CTA labels white over imagery; rule codified in no-bad-patterns.md (over imagery: white text + neutral scrims only); build green, VPS rebuilt + verified | open: Suzanne review; sitewide rollout must include the blue-on-image sweep (shop hero, events hero, speaking hero all use navy tints over video/photo)
2026-08-20 ~11:30 | web/design | Claude | Homepage impact pass after Johan rejected brightness pass as basic: all statement headlines font-light->font-semibold tracking-tight (root cause: weight-200 type vs reference site bold), hero rebuilt (1 credential line, 2-line 7xl headline, 2 CTAs), TrustBar stats 7xl count-ups, FocusAreas typographic index list, AboutTeaser offset accent block, duplicate headline fixed (Three ways to begin), design-rules.md headline standard updated; code-reviewer findings fixed; build green; deployed VPS 01ebb30, verified live desktop+375px | open: Suzanne review, sitewide rollout of both passes (colour + typography) pending approval
2026-08-20 ~12:30 | web/design | Claude | Impact pass round 2 per Johan (push further before rollout): FocusAreas desktop rebuilt as giant stacked-words editorial (TR pillars pattern), FeaturedPrograms rebuilt as poster cards (3/4 photo, black scrim, centered white lockup), new StatementBand section (Your pattern isn't your destiny), hero xl:text-8xl; review fixes (WCAG 2.2.2 focus-pause, h3 outline, scrim strength, aria-labels, xl codified size-scaling-only); deployed VPS 3163948, verified live 1440+375 | open: Suzanne review, sitewide rollout after approval, nav CTA gap 1024-1279 spawned as separate task
2026-08-20 ~17:15 | web/design | Claude | SITEWIDE ROLLOUT: six scoped agents applied the approved standard (light-first, no blue over imagery, bold statement type) to all public pages; hero 3rd CTA restored per Johan; build green, 686 component tests pass (28 API/data failures pre-existing on HEAD), zero md:/font-light-headline violations in diff; visual QA shop/about/services/events/programs/pattern-coach/topic/testimonials at 1440; deployed VPS 99289bc, all 8 key routes 200 | open: Suzanne full-site review, then PR to main; nav CTA gap task chip pending; portal+speaking excluded by design
2026-08-28 ~09:15 | web/design | Claude | HERO FOLD REGRESSION FIXED (Suzanne 27 Aug 19:56 "SR website" screenshot: huge writing, cant see whats underneath, small writing hard to read). Root cause: heroes were fixed-height (h-screen / lg:h-screen) + overflow-hidden with the content stack vertically centred inside. min-h-screen is also wrong here because the header is sticky and 80px IN FLOW, so 100vh always overshoots by 80px. Measured on the live dev server at 1280x720: hero stack needed 852px in a 720px box, 66px clipped off the top and the CTA row sitting 66px BELOW THE FOLD - reproduces her screenshot exactly. Fixes: (1) all four heroes (home Hero, AboutHero, ServicesHero, programs/[slug] ProgramDetailClient) now min-h-[calc(100vh-5rem)] so they grow instead of clipping; (2) homepage h1 keeps the approved impact-pass text-8xl but gated on [@media(min-width:1280px)and(min-height:960px)] so 96px only renders where there is vertical room, 72px below that; (3) the long hero supporting paragraph gated on min-height:780px - it is the least essential block so it yields first on short screens; (4) legibility, the "small writing" half of her complaint: font-light at white/70-75 over video was the least readable pairing on the page, now normal weight at white/80-90 across home/services/program heroes; (5) ServicesHero portrait capped max-h-[calc(100vh-16rem)] (it was a hard h-[520px] driving the whole section) and duplicate py-32 padding removed from services + program heroes. VERIFIED by measurement (browser pane not compositing, so no screenshots): all 3 homepage CTAs visible with 86px clearance at 1280x720, 39px at 1366x625, 150px at 1920x1000 with the full 96px headline restored; /about /services /programs-detail all clear the fold at 625px; mobile 375x812 clean, no horizontal scroll; nothing clipped at any size. tsc: 0 errors in the 4 changed files (167 pre-existing errors in 8 unrelated files). vitest: 699 pass / 15 fail, and the same 15 fail at HEAD with the changes stashed (lead-magnet route + privateSessions data, pre-existing). WHY THE ROLLOUT MISSED IT: the 20 Aug sitewide QA was run at 1440 wide on a tall viewport only, so no short-viewport case was ever exercised. | open: NOT deployed and NOT committed - Johan to review; separately confirmed the known nav CTA gap is real (at exactly 1280px wide the hidden xl:inline-flex header button appears but layout width is 1265 after the scrollbar, giving 16px of horizontal scroll on every page)
2026-08-28 ~12:55 | deploy | Claude | a037a35 DEPLOYED to review VPS. Server was on feature/homepage-brightness-pass at 759614d with a clean tree, so a plain fast-forward pull (no reset needed); infra-web rebuilt and recreated, container healthy, localhost 200. Verified on the LIVE site at 169.239.180.49, not just locally: at 1280x720 all three hero CTAs visible with 86px clearance and nothing clipped; at 1366x625 all visible with 40px clearance, no horizontal scroll; hero section exactly viewport-minus-header at both. Access note: this laptop could not SSH at first - the key was offered and the server said "Server accepts key" then denied, which is the signature of a passphrase-protected key under BatchMode. Git Bash ssh has no agent here; the WINDOWS ssh-agent service is running with id_ed25519 already loaded, so deploys from this machine must use C:\WINDOWS\System32\OpenSSH\ssh.exe, not the Git Bash ssh. | open: server-side git fetch prints "no such identity: /root/.ssh/github: No such file or directory" on every fetch - harmless today because another auth path succeeds, but the referenced deploy key is missing and should be tidied; .github/workflows/deploy.yml has a workflow_dispatch review-deploy job written and STAGED BUT NOT COMMITTED (needs Johan - it only takes effect once on the default branch anyway)
2026-08-28 ~13:30 | qa | Claude | FIX 13 CLICK-TEST run against the LIVE review site 169.239.180.49 (not localhost). Method: crawled every internal page reachable from the homepage plus targeted browser interaction; deliberately GET-only, no form ever submitted, because the contact form and the 8 quiz gates create real subscriber rows and send mail. PASSES: 146 internal pages crawled, ZERO non-200; every external link resolves except one (below); mobile menu opens and carries all 25 nav items including the 24/7 Coaching App entry; cookie banner sits in the bottom 88px and overlaps none of the 7 hero CTAs, so FIX 1 / KI027 still holds on live; site search returns real results (KI020 stays closed - note the payload key is results, not hits); contact form renders name/email/phone/enquiry/message with a submit control; no Careers route exists and nothing links to one, so FIX 9 is a content decision with nothing dangling. FAILURES: (1) BLOCKER - Add to Cart 500s for EVERY visitor. POST /store/carts 200 then POST line-items 500; Medusa logs "Cannot read properties of undefined reading calculated_amount" in core-flows get-variants-and-items-with-prices. GET /api/region returns the International/usd region even to a South African IP, and only 84 of 211 variants carry a USD price while all 211 carry ZAR - so the cart is USD, the variant has no USD price, Medusa throws. This is KI034 but much wider than recorded: not an international-visitor edge case, region detection defaults everyone to USD, so roughly 60% of the catalogue is unbuyable by anyone. Also found THREE regions with TWO duplicate South Africa/zar rows, a likely contributor to the misresolution. (2) FIX 12 still live and degrades at cutover - the external coach app still targets https://suzanneravenall.com/my-account/pattern-coach/ (old WordPress) with the real chat UI at display:none; that URL 404s once DNS moves. (3) footer "Built by" credit points at https://aidynamicadvisory.com which is DEAD, on every page. (4) booking CTA is http://169.239.180.49:3002/... raw IP + port + plain http, breaks at cutover. (5) KI001 confirmed still open - console shows Invalid Sentry Dsn on every page. Left behind: one empty Medusa cart cart_01M140QQ7PGNES004KTT6SQF3T with no line items (the add failed), harmless. | open: the cart blocker must be fixed before Suzanne is invited to review - she will click Buy

## 2026-08-28 - Shop blocker: region resolution (KI034) + cart RNaN (KI036)

Dug into the Add-to-Cart 500 found by the FIX 13 sweep. Root cause was not an
international edge case, it was every visitor.

`/api/region` read the visitor country from `CF-IPCountry`. Nothing in the stack
sets that header: DNS is at Xneelo (ns1.host-h.net / ns1.dns-h.com), there is no
Cloudflare proxy in front, and the VPS nginx is built without the geoip module
(`nginx -V` has no geo entries, no CF header handling anywhere in /etc/nginx).
So `country` was always `''`, the ternary read that as "not ZA", and every
visitor - South Africans included - was issued a USD cart. Only 84 of 207
variants carry a USD price, so adding any of the other 123 threw an unhandled
`Cannot read properties of undefined (reading 'calculated_amount')`.

Proved by probe: no header -> USD region, `CF-IPCountry: ZA` -> ZAR region.

Two corrections to the sweep report:
- The "three regions, two duplicate South Africa rows" is not a live data
  problem. One SA/zar row is soft-deleted (deleted_at 2026-05-05). Two live
  regions only.
- 207 live variants, not 211. 178 of those are exposed to the store API and all
  178 are priced in ZAR.

Fixes (a6bf940, 09c1a3c, 39ffca7 - deployed and verified live):
- `/api/region` resolves ZAR unless a geo header positively reports a non-ZA
  country; Cloudflare's XX (anonymised) and T1 (Tor) are treated as unknown.
- USD region gated behind `MEDUSA_USD_REGION_ENABLED`, default false, wired
  through docker-compose. Putting a CDN in front later cannot silently
  re-break the catalogue before USD prices exist.
- Persisted carts are validated against the region the visitor resolves to NOW.
  Previously any cart in `localStorage` matching *any* configured region was
  reused, so returning visitors would have carried the 500 across the fix.
- Found while verifying: cart and checkout rendered `RNaN` as every line total
  (correct order summary beside it). Medusa omits per-line subtotal/total
  unless requested by name. Cart responses are now normalised (line subtotal =
  unit_price x quantity) and `formatPrice` refuses to emit NaN. KI036.
- 14 new tests (9 region, 5 formatPrice), all green.

Verified live on 169.239.180.49 after rebuild:
- `/api/region` returns the ZAR region for no header, ZA, US and XX.
- API add-to-cart on the variant that reproduced the 500: HTTP 200, cart in zar,
  R165.00.
- Catalogue sweep: 98 products / 178 variants, 0 missing a ZAR price.
- UI: Add to Cart works, cart shows R165,00 line total, qty 2 -> R330,00,
  checkout summary R330,00. No NaN.
- Planted a USD cart id in localStorage, reloaded: discarded as intended, next
  add-to-cart created a fresh ZAR cart.

Checked and NOT a problem: the checkout "Billing country" selector only chooses
the payment gateway (PayFast vs PayPal). It does not switch cart currency, so it
is not a second route into the USD region. Consequence to flag: international
buyers are billed in ZAR via PayPal until USD prices are seeded.

Left behind: 4 abandoned test carts. Harmless, not deleted.
Local drift noted: laptop `infra/.env` is missing `NEXT_PUBLIC_MEDUSA_REGION_ID`,
which the server has. Local dev would fall through to the regions lookup.

## 2026-08-28 - Footer credit repointed (4b64d0e)

"Built by Ai Dynamic Advisory" in the site footer linked to
https://aidynamicadvisory.com, which does not resolve (curl returns 000). The
credit renders on every page, so a client site carried a dead link sitewide, on
our own name, right before we invited the client to click around it. Repointed
at https://www.aiautomations.co.za (200; the apex 307-redirects, so the www host
is the one to use).

Deployed and verified live: the footer href is the new host on /, /shop, /about
and /contact, and zero occurrences of aidynamicadvisory remain in the homepage
HTML.

NOT verified by unit test, on purpose and worth knowing: Footer.test.tsx cannot
run at all. All 27 tests fail with "React is not defined", identically with and
without this change (confirmed by stashing it). Header.test.tsx and
CookieConsent.test.tsx fail the same way, 15 more, and Header already has the
`import React` that Footer lacks. So it is a component-test environment config
problem, not a missing import, and it means there is currently NO unit-test
safety net on Footer, Header or CookieConsent. Worth fixing separately before
anyone leans on those suites.

2026-09-02 | web | EA session | WARM DIRECTION, homepage. Branch `feature/warm-direction`. The Friday 5 Sep commitment to Suzanne. Diagnosis carried over from 31 Aug: the palette (navy #012B43 + electric blue #1719F4) was inherited from the old WordPress site while the design reference has always been tonyrobbins.com, which is warm - so three brightness passes could never answer "it needs to be a warm site", because brightening a cold palette yields a lighter cold. TOKENS (packages/config/tailwind.config.ts + packages/ui/src/tokens.ts, kept in step): brand-primary 900 is now #1A1512 warm ink, brand-accent 600 #A84C07 warm amber, hover 700 #8F4108, plus new brand-cream #FDFAF6 (page ground), brand-sand #F5EDE3 (alternating band), brand-border #E7DED2, brand-ink/brand-muted warm text, and brand-blue #012B43 retaining Suzanne's navy as an IDENTITY colour only. accent-600 is deliberately the dark end of the amber ramp so ONE token passes WCAG AA on both light grounds rather than needing a per-background variant; accent-400 #F0A952 is the label colour on dark. Every pair contrast-checked before applying: white-on-accent 5.67, accent-on-cream 5.45, accent-on-sand 4.89, accent-400-on-ink 8.43, ink-on-cream 17.1, muted-on-cream 5.89, muted-on-sand 5.29. The three brain/pulse glow keyframes hardcoded the electric blue and were moved to #D97706. SWEEP across components/home/*.tsx + shared/MagazineCovers + shared/VideoTestimonials: 17 bg-white, 6 bg-gray-50, 5 dark greys, 16 gradient stops, 25 text greys, 5 borders. One deliberate exception, and it is the point of the design: WHITE CARDS STAY WHITE on the cream ground - that contrast is where the depth now comes from, so AboutTeaser's floating card, ServicesSection's cards and UpcomingEvents' cards keep bg-white while every section GROUND went warm. Design rules updated with the two rules that are easy to get wrong (white is a card colour not a section colour; accent on dark is the 400). VERIFIED in-browser: computed styles confirm CTA rgb(168,76,7), cream rgb(253,250,246), sand rgb(245,237,227), dark rgb(26,21,18), and ZERO elements still painting #1719F4 or #012B43 as a background; 0 console errors; mobile 375x812 all three hero CTAs above the fold, nothing clipped. tsc: 167 errors before and 167 after, all pre-existing in components/invoice (react-pdf typings), 0 in any file I touched - confirmed by stashing the change and re-running. | open: UNCOMMITTED and NOT deployed, awaiting Johan's eye - this is a client-facing direction decision, not a mechanical change. A bug worth recording: my sweep script keyed a protect-list on 'home/X.tsx' while Windows glob returned 'home\X.tsx', so the lookup silently returned nothing, the guard assert never ran, and the three white cards were swept anyway; caught on visual inspection and restored by hand. Two cream sections still sit adjacent (the FeaturedPrograms band and MagazineCovers), violating "never the same background twice in a row" - it pre-existed as white-on-white and MagazineCovers is SHARED, so changing it reaches other pages; left for the 15 Sep rollout. The hero photograph is still a cold blue stage shot and now fights the warm page - worth a warmer image or a warm overlay before Suzanne looks. Rollout to the remaining pages is the same mechanical pass as 20 Aug once she says yes.

2026-09-15 | web | design standard, branch `feature/design-standard` (off feature/warm-direction fec8240) | HOMEPAGE, pre-gate. Suzanne 14 Sep: headings too big and too bold, they block the background picture; not keen on the orange/brown. Johan 15 Sep: no orange/brown, not the old navy/electric blue either, reference tonyrobbins.com; smaller and lighter headers on every page; whole site on this by Tue 29 Sep; page by page, each deployed as it lands; change report every second working day (EA sends, this session never emails Suzanne); ONE gate before the first deploy. PALETTE read off the live tonyrobbins.com CSS variables and computed styles, sources per value in docs/DESIGN-STANDARD.md: it is MONOCHROME (--tr-black #000, --tr-black-1 #232325, --tr-black-2 #303134, --tr-white #fff, --tr-white-1 #F6F6F7, --tr-white-2 #DADBDF, --foreground #0A0A0A, --primary #171717, muted #696969, --border #E5E5E5); its one hue, --tr-blue #2764FF, is on the announcement bar only. Type: Suisse Intl weight 500, hero 70px desktop / 48px at 375, section 57.6 / 39.5px, hero has NO description paragraph. TOKENS (both files in step, names kept, cream/sand now mean white/light grey): primary ramp to black, accent = near-black #171717 (CTA and label on light), accent-400 #DADBDF (label on dark), cream #FFF, sand #F6F6F7, border #E5E5E5, ink #0A0A0A, muted #696969, glow keyframes white. HEADER RULE (design-rules.md + DESIGN-STANDARD.md): h1 36/48/60px font-normal, one description text-sm/base/lg white/85, black scrim only behind the text band (black/65 to /85; worst case over pure white: white 6.98, white/80 5.18), content bottom-left; below sm STACKED because the hero video is a face close-up (picture top 320px, text on black, fade meets the ground exactly at 320). The hero's second paragraph was dropped (Johan's "less info"). Section headlines 600 to 500 = the measured reference weight. FIXES the swap forced: every bg-brand-accent on a dark ground or photo would ghost (near-black on black), so white CTAs in Hero, FinalCTA, LeadMagnet, BookPromotion badge, UpcomingPrograms cards, Header nav CTA, CookieConsent Accept, MobileNav CTAs + focus rings + active state. PatternCoachTab (on every page) still hardcoded 12x #1719F4 and blue glows from before the warm pass: now currentColor / neutral. MagazineCovers got a `tone` prop, dark on / and /about, to end same-ground adjacency. UpcomingEvents badges were orange (BOOK) and emerald-400 on white at ~1.9:1: now neutral. Two accreditation logos (AADP, Royal Society of Medicine) are white artwork and were INVISIBLE on the live site too: now inverted. Electric-blue hover glows removed from 5 components. VERIFIED: scripted contrast scan of every text node at 1280 and 375, 0 real failures and 0 ghost buttons (the remaining flags are text on sibling photos, checked by eye in full-page slices); grounds alternate with no repeat; 375: h1 36px/400, all three hero CTAs above the fold (bottoms 672/726/780 of 812), no horizontal scroll; tsc 167 = baseline, 0 in touched files (NB the 2 Sep note that all 167 are in components/invoice is wrong, they are spread across API tests, portal and explore); layout tests 71/71 pass, identical with the four layout files stashed, so the 28 Aug note that Footer/Header tests cannot run is stale. code-reviewer: 4 findings fixed, 1 deferred (below). TOOLING: the in-app browser pane freezes framer-motion (hidden pane, no rAF) and headless --screenshot catches fades mid-way, so infra/scripts/qa/cdp-shots.mjs drives chrome-headless-shell over CDP with real waits (viewport slices, before/after for reports) and infra/scripts/qa/contrast-scan.js is the scanner. | open: NOT committed, NOT deployed, waiting on Johan's one-time gate. The token swap is sitewide by nature, so the first deploy recolours every page at once; the per-page header passes follow it. Untouched and not mine: .github/workflows/deploy.yml (uncommitted workflow_dispatch review-deploy job), stray root PNG. For Johan: hero image still open with Suzanne; Suisse Intl, pill buttons and sentence-case buttons not adopted (his call); Brilliant Coach pill covers the third hero CTA on phones once consent is given (pre-existing); TestimonialSpotlight will repeat a neighbour's ground whichever tone it gets once a verified story exists; "International Client" testimonial thumbnail is broken (pre-existing); the project memory file reference_vps.md holds the VPS root password in plain text.

2026-09-15 (later) | web | design standard | GATE MOVED TO A DESIGN CANVAS. Johan asked why we were not using Claude's design tool; the site had been built code-first, so every round of feedback landed on deployed code. Built a Claude Design canvas (private artifact https://claude.ai/artifact/DBFdgn8HRYYDv68aGre1mZ, owner-only until he shares it) for the one pre-deploy gate: page 1 homepage hero A (as committed in 746c797) / B (A without the "Dr. Suzanne Ravenall · Founder of Pattern Intelligence" eyebrow, recommended) / C (tonyrobbins.com pattern: headline + one button), each at 1440 and 375, with a Picture tweak (stage video poster vs studio portrait, since the hero photo is open with Suzanne); page 2 the inner page-header template applied to About (60px regular, one-line description, bottom band scrim, stacked on phones; the header quote moves out because MediaLogos already carries it); page 3 palette, type and contrast sheet from docs/DESIGN-STANDARD.md, with the not-adopted list as a note. Values copied from Hero.tsx, Header.tsx, DesktopNav.tsx and the Tailwind preset. Working files in the session scratchpad (canvas/); the live canvas can be extracted back to files if they are lost. DesignSync (design-system sync with claude.ai/design) needs /design-login from an interactive terminal; not set up. | open: waiting on Johan's pick of A/B/C and his OK on the About template and palette. On his pick: code the winner into Hero.tsx, build the page-header pattern (AboutHero first), production build, deploy, then the Thu 17 Sep report. Correction to the entry above: the homepage work IS committed (746c797, local, not pushed); still not deployed.

2026-09-15 (deploy) | web | design standard | FIRST DEPLOY, homepage option B. Johan picked B on the design canvas ("B"); taken as the gate OK for the palette too, since the palette sheet was on the same canvas (rollback = redeploy fec8240). Hero.tsx: the "Dr. Suzanne Ravenall · Founder of Pattern Intelligence™" eyebrow removed; header rule (design-rules.md + DESIGN-STANDARD.md) now makes the eyebrow optional and bars one that repeats the logo. tsc 167 = baseline, 0 in Hero; production build green (4GB heap). Commit 444b4d0, branch pushed to origin (new). VPS switched from feature/warm-direction (fec8240) to feature/design-standard, ff pull, docker compose build web + up -d web. VERIFIED LIVE: / /shop /about /contact /events all 200; all 8 containers healthy; hero-heading present, name line gone from the page (the phrase survives only in the Person JSON-LD description, which is SEO data, not rendered); live screenshots at 1280x800 and 375x812 match the canvas, all three CTAs above the fold on the phone and the Brilliant Coach pill now sits below the third CTA instead of over it. Because the tokens are sitewide, every page on the review box is now monochrome; only the homepage has the new header so far. | open: Thu 17 Sep report (docs/progress/2026-09-17.md, before/after shots in the scratchpad shots/ folder); next page About on the canvas template, where the "Meet Dr. Suzanne Ravenall" label also repeats the logo and by the B rule should go (proposed to Johan); the Person JSON-LD description carries an em dash (existing copy, not changed).

2026-09-15 (about) | web | design standard | /about DEPLOYED (16884bf). Johan: "Yes. do the about page", including the proposal to drop the "Meet Dr. Suzanne Ravenall" eyebrow (repeats the logo). AboutHero rebuilt on the canvas template (bottom band, 36/48/60px regular, one description, stacked below sm with the brain-video crop shifted to object-[30%_50%] so her face stays in frame); the header quote moved out because MediaLogos carries it on the same page. Beyond the header, the token swap had broken: AboutFinalCTA primary button (near-black on a dark photo, invisible) now white; HowItWorks/TheEcosystem dark cards were blue-tinted gray-900, now black; grey classes to brand tokens; h2s medium. Grounds re-sequenced so nothing repeats (How It Works to white; VideoTestimonials got a `tone` prop, sand on /about, cream default elsewhere). Code review PASS WITH NOTES: the one real finding (VideoTestimonials play buttons were the near-black accent on photos, also on the homepage) fixed, now white. VERIFIED: tsc 167 = baseline; contrast scan 133 text nodes, 0 fails, 0 ghost buttons; grounds alternate end to end; local and live screenshots at 1280 and 375. DEPLOY NOTE: first attempt pushed nothing (GitHub 504) and the VPS build of the old commit hung with ~0 CPU for 10+ min; killed it (web container untouched), re-pushed, rebuilt with --progress=plain: 190s turbo build, all 8 containers healthy, / /about /shop /contact /events /about/the-story all 200. Before/after shots in the scratchpad shots/ folder (before-about-*, after-about-*, live-about-*). | open: About subpages (the-story, the-system, the-science) rebuilt on the new shared components/shared/PageHeader.tsx, verified locally, not yet deployed; Thu 17 report.

2026-09-15 (about subpages) | web | design standard | /about/the-story, /the-system, /the-science DEPLOYED (574cfa0). New shared components/shared/PageHeader.tsx (named export + HEADER_UNDERLINE) implements the header rule for photo headers; design-rules.md now points every inner photo header at it. The three inline heroes had the stage photo at 50% under a 75-90% full-frame wash (the picture Suzanne said was blocked out); now full strength with the band scrim only. Wording verified byte-identical against HEAD (eyebrow, headline, description on all three). Their final CTAs had the same invisible near-black button: now white. Grey classes to tokens, h2s medium. Code review PASS WITH NOTES (both low: copy check, done; literal-only note on mobileCrop, added). VERIFIED: tsc 167 = baseline; contrast scans the-story 104 / the-system 89 / the-science 102 text nodes, 0 fails, 0 ghost buttons, grounds alternate; local screenshots at 1280 and 375; live: push confirmed on origin before deploy, 187s build, all 8 containers healthy, / /about + three subpages /shop /contact /events all 200, old 72px semibold hero class absent from live HTML. | open: next pages by traffic (programmes/shop, events, contact, legal last); Thu 17 report.

2026-09-15 (about, text right) | web | design standard | Johan: "On the about hero section I think we should move the captions to the right." In the About video Suzanne stands on the left and the brain fills the right, so bottom-left text sat over her. From sm the headline and description now sit in a right-hand column (sm:ml-auto sm:max-w-md lg:max-w-2xl), still left-aligned inside it (ragged-left lines read worse); phones unchanged (already stacked, picture above the text). First try used sm:max-w-xl: at 768 the column's left edge ran into her hair and shoulder, so the tablet column is narrower (448px, five-line headline, fits the 560px section). Header rule in design-rules.md and DESIGN-STANDARD.md now allows bottom-right when the subject stands on the left. Checked by screenshot at 375, 768, 1024, 1280 and 1440; tsc 167 = baseline. | open: deploy; the design canvas About board still shows bottom-left.

2026-09-15 (programmes, shop, checkout, events, contact) | web | design standard | Johan: "Carry on". Next pages by traffic, done as four disjoint batches: three helpers (programmes; shop; cart+checkout+confirmation, className-only) and events+contact by hand. PageHeader gained video {src, poster} and align 'right' (AboutHero column) so the shop video header and future subject-on-left pictures use it. HEADERS: /programs, /programs/[slug], /shop (video), /events onto PageHeader; extra header content moved to a slim black band below (programme facts: back link, badge, duration, price; shop stats); shop "Scroll" hint dropped; ProductHero rebuilt inline to the rule (needs its breadcrumb). /contact keeps its flat centred header (no picture): sand ground, medium weight. FIXES the token swap left: near-black CTAs on photos (events, programmes, shop final CTAs, product highlight badge) now white; blue-tinted gray-900/950 cards now black; event/shop category badges (emerald/amber/purple) neutral like the homepage; checkout contrast: red-500/400 text and borders to red-600, payment error text red-700, completed step circles/connector emerald-500 (2.5:1) to black, VariantSelector "Added" emerald-600 to 700, add-to-cart error text red-600 on light; contact error text red-600; orange-era glows removed (contact final CTA, programme detail CTA); programmes sticky category bar to sand so it does not merge with the white section below. Kept on purpose: PayPal button hex (PayPal's brand; comment added, flagged for Johan); confirmation green tick (passes 3:1). VERIFIED: tsc 167 = baseline; ProductCard test 6/6; two code reviews PASS WITH NOTES (all real notes fixed; black facts band under the programme header is intended); new infra/scripts/qa/cdp-scan.mjs batch scanner: at 375 events/contact/programs/detail/cart/checkout/confirmation/the-story/shop and at 1280 events/contact/shop all 0 below AA, 0 invisible buttons; screenshots at 1280 and 375 checked by eye. NOT verifiable locally: product pages (Medusa unreachable from the laptop, /shop/<handle> 404s and the grid shows "Unable to load programmes"): check on the review box after deploy. | open: deploy this batch and check a live product page; legal pages next; Thu 17 report draft in docs/progress/2026-09-17.md.

2026-09-15 (batch live) | web | design standard | 15f612b DEPLOYED: push confirmed on origin, VPS build clean, 8/8 containers healthy; / /about /programs /programs/trauma-to-transcendence /shop /shop/rapid-repatterning-session /events /contact /cart /checkout all 200. The product page (not renderable locally) checked live: ProductHero breadcrumb + headline + description on the bottom band at 1280 and 375, variant pills and Add to Cart near-black on white; cdp-scan live /shop and the product page at 1280 and 375: 0 below AA, 0 invisible buttons. Note for future checks: /shop renders its grid client-side, so product links are not in the server HTML; take a handle from the Medusa data or use rapid-repatterning-session. Next wave started: four helpers on services/speaking/masterclass, explore/pathways, resources/blog/book, testimonials/quiz/pattern-coach/community/search (pattern-hub components included). | open: review and deploy the wave; legal pages last; Thu 17 report.

2026-09-15 (legal, 404, error) | web | design standard | Legal pages (terms, privacy, cookies, disclaimer), done by hand while the four helpers work on disjoint areas: flat black header kept (no picture), h1 font-light to font-medium, eyebrow text-brand-accent-400, date lines gray-300 (blue-tinted) to white/70; content ground bg-white to cream; headings gray-900 semibold to brand-primary medium; long legal prose gray-600/700/800 to text-brand-ink for reading (not muted); cookies tables border-brand-border, head cells bg-brand-sand. cdp-scan all four at 375 and 1280: 0 below AA, 0 invisible buttons, 0 same-ground pairs; screenshots of cookies (table) and terms checked. A first terms desktop capture showed the 404 page: dev-server recompiling while helpers edit files; retake was fine (4543px page). not-found, error and global-error were still on the old grey-900 look: now cream ground, flat headline font-medium brand-primary, text-brand-muted, standard near-black CTA, p-24 to px-4 py-24 (96px side padding crushed the text at 375). 404 at 375 scanned clean and looks right; at 1280 both the capture and the scan return nothing (page reloads mid-evaluation in the dev server), so check the live 404 at desktop after deploy. | open: the four helper areas; one tsc run after they finish; deploy the wave with legal and error pages; live desktop 404 check.
