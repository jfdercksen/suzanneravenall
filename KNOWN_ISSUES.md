# Known Issues — Suzanne Ravenall Platform

Track all known issues here. Resolve by linking to the commit or PR that fixes them.

| ID | Issue | Severity | Location | Reported | Status |
|---|---|---|---|---|---|
| KI001 | Sentry account created but DSNs cannot be finalised until DNS cutover | Medium | All .env files | March 2026 | Deferred to Phase 5 |
| KI002 | Backblaze B2 account not yet created — backup cron not installed on VPS | High | infra/scripts/backup.sh | March 2026 | Resolved (April 2026) |
| KI003 | All change_me placeholder values still in production .env — must be replaced before Phase 1 | Critical | .env (VPS) | March 2026 | Resolved (April 2026) |
| KI004 | Payload CMS admin CSS not loading and login redirects back to login screen on VPS Docker deployment | High | infra/docker-compose.yml, apps/payload/Dockerfile, infra/nginx/conf.d/ | April 2026 | Resolved (2026-06-11, e7139ee) — the April changes (basePath + env var names) fixed the CSS/routing, but the login bounce persisted. Actual remaining cause: nested `<html>` tags — root `apps/payload/src/app/layout.tsx` rendered its own `<html>/<body>` while the `(payload)` group's `RootLayout` renders its own, breaking React hydration (#418) so the login form's server action never ran. Fixed by making the root layout a pass-through. `PAYLOAD_PUBLIC_SERVER_URL` was a red herring (it is not read for `serverURL`). Verified on VPS: single `<html>`, login works in incognito. |
| KI005 | Payload CMS basePath routing conflict — without basePath Nginx /cms prefix breaks routing; with basePath React hydration error #418 prevents form interaction | High | apps/payload/next.config.ts, infra/nginx/conf.d/ip-testing.conf | April 2026 | Resolved (2026-06-11, e7139ee) — corrected root cause: the #418 hydration error was NOT an empty serverURL/basePath problem. `basePath: '/cms'` (in `next.config.mjs`) and `PAYLOAD_URL` were both correct. The real cause was duplicate root layouts rendering nested `<html>` tags (root `app/layout.tsx` + the `(payload)` `RootLayout`). Fixed by making the root `app/layout.tsx` a pass-through so Payload's `RootLayout` solely owns `<html>/<body>`. |
| KI006 | 15 legitimate products in WooCommerce category 73 ("not used-Akashic Coaching") excluded from Task 2.3 migration | Medium | apps/medusa/src/scripts/migrate-woocommerce.ts | April 2026 | Fixed in code (2026-08-11) — pending VPS re-run. All 15 now covered by CONSOLIDATION_MAP entries: RR Program 6 Inner Cultivation (new product, Live R5,315 + Self Study R4,205), RR Program 7/9 gain a Live via Zoom variant (R5,315 each, added to the EXISTING products via the new variant-sync step), Deep Energy Clearing Level 1 & 2 Purchased Together (Live R17,500 + Live Retaker R1,660), Practitioner Mentorship (4 variants, R575/R1,900, collection practitioner), Email Support R200, Coaching Support Package R1,660, VIP Package R1,500, Bonus Lifetime Access R2,060 (start-here), Ravenall Institute Certification Observation Fee R2,800 (practitioner). All new variants manage_inventory:false (none are group-session Live seats). Idempotent: products matched by handle and skipped; missing variants added by title, so re-runs never duplicate. Deliberately still excluded: WC 17133 "Suzanne Ravenall Coaching App" (legacy app-subscription stub, no description/image) and WC 13795 (already absorbed by the Full Basic Series product). 34 unit/integration tests in apps/medusa/src/scripts/__tests__/migrate-woocommerce.test.ts. TO CLOSE: Johan runs the migration on the VPS (dry run then DRY_RUN=false, see BUILD_STATUS session note), then re-runs infra/scripts/update-product-descriptions.mjs and infra/scripts/migrations/set-product-thumbnails.mjs so the new products get curated copy + site-local thumbnails (the add-on products may need images added to apps/web/public/images/products/ or OVERRIDES entries). redirect-map.json gains new entries on the run; note verify-migration.mjs still expects the old counts (48 products / 129 redirects) and will report a benign MISMATCH. |
| KI007 | seed.ts fails on Medusa v2 tax-rate API (region_id filter not supported) — regions were created but tax rates, shipping options, and 12 placeholder products were not seeded | Low | apps/medusa/src/scripts/seed.ts | April 2026 | Open — seed.ts needs tax-rate query fixed for Medusa v2 before placeholder products can be seeded. Collections were created separately via seed-collections.mjs. |
| KI008 | Task 2.3 WooCommerce migration applied consolidation — 128 WC flat products consolidated into 48 Medusa products with variants. 129 URL redirects written to infra/scripts/migrations/redirect-map.json for Task 1.9 | Info | apps/medusa/src/scripts/migrate-woocommerce.ts, infra/scripts/migrations/redirect-map.json | April 2026 | Resolved (April 2026) — consolidation approach replaces the original flat 1:1 WC→Medusa mapping. See CONSOLIDATION_MAP in migrate-woocommerce.ts for full variant spec. |
| KI009 | MEDUSA_DATABASE_URL must include ?sslmode=disable — without it the pg driver attempts SSL against the internal Docker Postgres container which does not have SSL enabled, causing all module migrations to fail with "The server does not support SSL connections" | High | infra/.env (VPS), .env.example | May 2026 | Resolved (May 2026) — ?sslmode=disable appended to MEDUSA_DATABASE_URL in infra/.env on VPS. Document all future Medusa deployments must include this flag. |
| KI010 | Sage Business Cloud credentials not yet obtained — Sage integration (Task 2.8) is built but cannot be activated until credentials are provided | High | infra/.env (VPS) | May 2026 | Open — requires Suzanne to provide: SAGE_EMAIL (Sage login), SAGE_PASSWORD, SAGE_API_KEY (from Sage developer settings), SAGE_COMPANY_ID (from GET /Company/Get). Also need SAGE_INCOME_ACCOUNT_ID (from Chart of Accounts) and MEDUSA_API_TOKEN (from Medusa admin API Key Management). Once credentials are in infra/.env, rebuild medusa + n8n containers and import + activate infra/n8n/workflows/medusa-order-to-sage.json in the n8n UI. |
| KI011 | Discourse community not deployed — VPS has insufficient RAM (1.6Gi available, Discourse requires 2GB minimum). Placeholder /community page with email capture in place. | High | infra/docker-compose.yml | May 2026 | Open — deferred to post-launch (D014). Fix: upgrade VPS to 8GB RAM then follow IMPLEMENTATION_WORKFLOW.md Task 3.8 to deploy Discourse and wire Supabase SSO. No code changes required beyond adding the Docker service and SSO endpoint. |
| KI012 | Wild Apricot member migration (Task 3.8) on hold — awaiting Suzanne's confirmation of whether there are active members to migrate and provision of Wild Apricot API credentials (accountId + API key from Settings → Integrations → Wild Apricot API). | High | infra/scripts/migrations/ | May 2026 | Open — blocked on client response. Once confirmed: spawn migration-agent with Wild Apricot credentials. If no active members to migrate, close this issue and mark Task 3.8 N/A. |
| KI013 | Vtiger CRM credentials not yet obtained — VtigerService (Task 4.1) is built but cannot be activated until Suzanne provides a Vtiger instance URL and credentials | High | infra/.env (VPS) | May 2026 | Resolved (2026-06-09) — VTIGER_URL, VTIGER_USERNAME, VTIGER_ACCESS_KEY added to infra/.env on VPS. VTIGER_* vars also added to n8n service in docker-compose.yml (were missing — n8n workflows read via $env.*). Rebuild n8n container on VPS to pick up new vars: `docker compose -f docker-compose.yml up -d n8n`. Vtiger custom fields (cf_pipeline_stage, cf_membership_tier, cf_total_spend_zar, cf_last_purchase_date, cf_discovery_call_date) still need to be created in Vtiger admin → Module Manager → Contacts before pipeline stage updates will succeed. |
| KI014 | Medusa admin API (/api/admin, /admin) not restricted at Nginx level before DNS cutover | High | infra/nginx/conf.d/suzanneravenall.conf.disabled | May 2026 | Open — pre-DNS-cutover requirement. Add a Cloudflare WAF custom rule to restrict /admin/* and /api/admin/* to office IPs before going live. Both Nginx configs have TODO comments noting this. Medusa JWT auth is still active; this is defense-in-depth. |
| KI015 | next.config.mjs image remotePatterns contains placeholder Supabase hostname ('your-project.supabase.co') | High | apps/web/next.config.mjs:38 | May 2026 | Resolved (Task 5.5) — replaced with real hostname: mjhwonoekokxyisfljtj.supabase.co |
| KI016 | /portal/account first-load JS is 310 kB — exceeds 200 kB threshold | Medium | apps/web/app/portal/account | May 2026 | Resolved (2026-08-11) — first-load JS reduced 311 kB → 208 kB (shared-by-all baseline is 204 kB, so only ~4 kB is page-specific; the 200 kB budget cannot be met by any route without shrinking the shared chunk). Fix as recommended: AccountContent.tsx split into ProfileForm.tsx + PasswordForm.tsx (client) with Membership Details and Danger Zone server-rendered in page.tsx; framer-motion entrance animations replaced by scoped CSS (account.module.css, honours prefers-reduced-motion); @supabase/ssr browser client now lazy-imported inside submit handlers and inside PortalNav's logout handler (interaction-gated, no behavior change). |
| KI017 | console.error/console.warn in payment-critical API routes not forwarded to Sentry | Medium | apps/web/app/api/webhooks/*, apps/web/app/api/invoices/generate | May 2026 | Resolved in code (2026-08-12, feature/redesign-pass-1) — new helper apps/web/lib/log.ts (logError/logWarn: console output unchanged + Sentry captureException/captureMessage with context extras, wrapped so a Sentry failure can never break the calling path). All 60 remaining console.error sites across 18 apps/web files (payfast/paypal/bunny/calcom webhooks, paypal checkout+capture, invoices/generate, all email routes, search, video, contact, signup-sync, lib/email/suppression) converted; 2 payment-loss console.warns (PayFast FAILED/CANCELLED, PayPal CAPTURE.DENIED) upgraded to logWarn, closing their Phase-5 TODOs. Deliberately unchanged: lead-magnet + quiz routes (6 sites already paired with Sentry.captureException). Errors reach Sentry only once DSNs are set on the VPS (KI001) — without a DSN everything no-ops to plain console. apps/medusa/apps/payload out of scope (no Sentry integration there). |
| KI018 | PAYFAST_SANDBOX and PAYPAL_SANDBOX env vars not yet set on VPS | High | infra/.env (VPS) | May 2026 | Partially resolved (Task 5.5) — PAYFAST_SANDBOX=true and PAYPAL_SANDBOX=true added to infra/.env for IP testing phase. Must be changed to false before DNS cutover to go-live. |
| KI019 | VPS Docker image is stale — /about and /contact return 307 self-redirect loops; /blog, /community, /legal/* return 404 | Critical | VPS Docker (web container) | May 2026 | Resolved (a6088e0) — root cause was a stale `apps/web/node_modules/next@15.3.3` entry in the lockfile coexisting with root `next@15.3.9` (from the `"next": "15.3.9"` override). In Docker partial-workspace builds, `npm ci` installed both versions. During static generation of /404, the two Next.js instances caused the Html guard to throw. Fixed by pinning `apps/web` next to `15.3.9` and removing the stale lockfile entry. Also fixed zod missing dependency (commit 21be042). Web container rebuilt and healthy — /about and /contact return 200. |
| KI020 | MeiliSearch returning 503 "Search unavailable" — search index not seeded | High | VPS — MeiliSearch | May 2026 | Open — seed script requires MEDUSA_API_TOKEN to fetch products from Medusa admin API. Fix: (1) Log into Medusa admin at http://169.239.180.49/api/admin → Settings → API Key Management → Create API Key. (2) Add MEDUSA_API_TOKEN=<key> to infra/.env on VPS. (3) Run: docker compose -f infra/docker-compose.yml exec medusa npx ts-node src/scripts/seed-meilisearch.ts. SearchBar shows graceful "No results" empty state so UX is not broken. |
| KI021 | Social media links in Contact page and Footer use placeholder hrefs (#) — no real Suzanne Ravenall profiles linked | Low | apps/web/app/contact/ContactOptions.tsx, apps/web/components/layout/Footer.tsx | May 2026 | Resolved — Facebook, LinkedIn, Instagram updated with real URLs. Location updated to Kyalami, GP. Email updated to support@ravenallinstitute.com. YouTube placeholder remains (no URL provided). Correction (2026-07-29, 3bc7c7b): the May fix put a wrong email (support@ravenallinstitute.com) and wrong Instagram handle (ravenallinstitute) on the Contact page's "Other Ways to Connect" card — neither appears on the real old site. Corrected to the verified real values (sravenall@suzanneravenall.com, instagram.com/suzanneravenall) already used correctly in the Footer. |
| KI023 | LeadMagnet section promises "The Breakthrough Masterclass" — asset does not exist | High | apps/web/components/home/LeadMagnet.tsx | May 2026 | Resolved (feature/homepage-restructure) — replaced masterclass offer with "Download Chapter 1 of The Breakthrough Trilogy" offer. Email capture API endpoint unchanged. Real PDF delivery pending Suzanne providing the chapter content. TODO comment left in LeadMagnet.tsx. |
| KI027 | On the smallest phone viewports (iPhone SE, 375x667), CTA buttons near the bottom of the viewport can be partially/fully overlapped by the fixed-bottom CookieConsent banner (+ Pattern Coach pill) on first visit — confirmed via visual QA on the homepage hero (2nd/3rd CTAs) and reproduced again on /shop (ShopHeroBanner CTAs). This is a global fixed-bottom-overlay stacking issue, not a per-page hero bug — every page is potentially affected on first visit at this viewport size, not just the homepage. On 390x844+ (iPhone 14 and larger — the large majority of real devices) CTAs are generally clear. | Low | apps/web/components/layout/CookieConsent.tsx, apps/web/components/layout/PatternCoachTab.tsx (affects any page's hero, not a single component) | 2026-07-29 | Fixed pending review (2026-08-11, feature/redesign-pass-1) — mobile Pattern Coach pill now waits until the cookie banner is answered (consent event + localStorage check), banner compacted to a single inline row below 640px (shorter copy, min-w-0, tighter Accept padding) with env(safe-area-inset-bottom) padding, "IN YOUR POCKET" span hidden ≤414px so the pill fits a 375px viewport, z-order made deliberate (banner z-[70] above pill z-[60] above header z-50). Needs visual QA at 375x667. Original root cause: at 375px width the cookie banner's Reject/Accept buttons wrap below its text (banner ~141px tall), and combined with the Pattern Coach floating pill (appears 2s after load, also fixed-bottom on mobile), the two overlays leave very little vertical room free after the fixed header. Possible next steps: shorten the cookie-consent copy on the smallest breakpoint so its buttons stay inline with the text (avoids the wrap that adds ~40-50px), and/or don't show the Pattern Coach pill while the cookie banner is still undismissed (avoid stacking two first-visit overlays at once). Confirmed NOT a hero-layout bug — DOM measurements show hero content itself is never clipped, only visually covered by these overlays. |
| KI026 | text-brand-accent (#1719F4) fails WCAG AA contrast on dark backgrounds — ~2.5:1 on bg-gray-900/950 vs the 4.5:1 minimum. Reads fine on white (~8.5:1), which is why it wasn't caught earlier. Fixed for the 14 image-hover card components (product/program/topic/session cards, search results) on 2026-07-29 by swapping to the lighter text-brand-accent-400 (~5.8:1 contrast) — see tailwind.config.ts accent-400. The same DEFAULT-on-dark pattern still recurs ~400+ times across 116 files, mostly as static "eyebrow" section labels (text-xs uppercase ... text-brand-accent) on dark section backgrounds — those were left untouched pending a dedicated sweep. | Medium | apps/web (sitewide) | 2026-07-29 | Resolved (2026-08-12, feature/redesign-pass-1) — full sitewide sweep done. Inventory: 392 bare `text-brand-accent` instances across 115 files, every one classified against its actual rendered background. Fixed 224: 105 → text-brand-accent-400 on gray-900/950/black and dark image overlays (4.89–5.79:1, AA pass), 119 → text-brand-accent-300 on bg-brand-primary*/gray-800 (5.73–6.06:1) because accent-400 only reaches 3.83–4.05:1 there (sub-AA for normal text; contrast math in BUILD_STATUS 2026-08-12 note). accent-300-on-brand-primary matches the existing quiz-flow precedent (QuizGate/QuizFlow). Also corrected 8 sub-AA leftovers FROM the original 14-instance fix that sat on brand-primary-family bgs (TopicRecognition, PathwayDetail x3, PathwaysGrid hover x2, VariantSelector x2 — now -300). VariantSelector renders in both light and dark product-page sections, so its eyebrow + spots-left line became `dark ?` conditionals (color classes only). Left as-is 168: 163 on white/gray-50 backgrounds where DEFAULT passes (8.46:1 on white), and 5 accepted-risk decorative watermark glyphs at /15–/40 opacity on dark (PatternHubQuote, PatternHubHowItWorks, TestimonialsSection quote mark, TopicApproach step numeral, BookContent book numeral) — aria-hidden/select-none decorative text, exempt from 1.4.3, left at their designed opacity. ContactForm success message left bare: its only usage passes `light`. Verified: 167 tsc errors (= baseline), 15 test failures (= baseline; one extra search-route timeout was load-flake, passes in isolation). No test asserted the changed classes. Palette tokens untouched. |
| KI025 | /resources/media "Read" links point to the OLD live WordPress site (e.g. suzanneravenall.com/article-leadership-magazine/) instead of hosting the article content on the new site | Medium | apps/web/data/mediaArticles.ts (was components/resources/MediaContent.tsx + ArticlesContent.tsx) | 2026-07-29 | Partially resolved (2026-08-11, feature/redesign-pass-1) — no old-WP link can reach a visitor anymore: all 10 press-article entries (6 on /resources/media + 4 more in the orphaned ArticlesContent.tsx, which shared the same list) extracted to a single typed source `apps/web/data/mediaArticles.ts` with `status` ('external' \| 'needs-content-decision') and `legacyHref` (original WP URL, kept for one-line restore, never rendered). All 10 are `needs-content-decision` with NO `href`, so both components now render them as static citation cards (no anchor, no href="#"). Guard test `data/mediaArticles.test.ts` fails the build if any rendered href matches suzanneravenall.com//wp-content//?p= patterns. STILL OPEN — content decision per article for Suzanne/Johan: (1) obtain full text and host natively, (2) link a web.archive.org snapshot, or (3) drop the entry. Once decided, set `href` + `status: 'external'` (or an internal route) per entry in mediaArticles.ts; nothing else needs editing. |
| KI024 | Old site's Contact page had a second "Careers Portal" tab ("Want to be a part of the Human Performance & Health revolution?") — a recruiting form for joining HPR, a separate group company. Not rebuilt on the new /contact page. | Low | apps/web/app/contact/ | 2026-07-29 | Open — deferred by Johan, add later. Content: heading "Careers Portal", tagline "Want to be a part of the Human Performance & Health revolution?", shared "Leave your details below and we will contact you!" form (name, last name, email, contact number). Old page: https://suzanneravenall.com/contact/ |
| KI028 | The new gated diagnostic-quiz flow's `/explore/{slug}/quiz` page does a live Supabase service-role token lookup on every GET, but has no rate limiting — unlike `/api/quiz/subscribe` and `/api/quiz/complete`, which both have an in-memory per-IP limiter. `/explore/*` also falls outside the existing Cloudflare WAF rule scoped to `/api/*`, so this route is currently unprotected against request-flooding (not a brute-force risk — the access token is a 256-bit random value — but an uncovered DB-load/availability vector). | Medium | apps/web/app/explore/[slug]/quiz/page.tsx, Cloudflare WAF rules | 2026-07-29 | Resolved (2026-08-11, feature/redesign-pass-1) — in-app limiter shipped. New shared util apps/web/lib/rate-limit.ts (fixed-window in-memory per-IP, extracted from the inline copies; single-container deployment, swap for Redis before scaling out). page.tsx now rate-limits the tokened GET at 30 req/min/IP before the Supabase lookup (a server component cannot return a real 429, so over-limit requests get a graceful "too many requests" retry screen with the DB lookup skipped — the availability vector KI028 flagged). /api/quiz/subscribe and /api/quiz/complete migrated onto the shared util keeping their 5 req/10min/IP limits and now return 429 WITH a Retry-After header; quiz UI (QuizGate/QuizFlow) shows friendly messages on 429, never crashes. Tests: lib/rate-limit.test.ts + under-limit/over-limit/window-reset cases in both route tests and a new page.test.tsx. Still worth adding the Cloudflare WAF rule for /explore/*/quiz* as defense-in-depth once the Cloudflare MCP is authenticated; /api/search and /api/email/unsubscribe still carry their own inline limiters (no Retry-After) and can migrate to the shared util opportunistically. |
| KI030 | Sitewide checkout broken — every product variant was created with Medusa v2's default manage_inventory=true (no creation path passed the flag) while no stock location was ever linked to a sales channel, so complete-cart throws "Sales channel {id} is not associated with any stock location for variant {id}" for EVERY product, not just group sessions. Only the 8 group-session "Live" seats are supposed to track inventory (12-seat cap). | Critical | apps/medusa/src/scripts/ (all product-creation paths) + Medusa DB on VPS | 2026-08-04 | Open (code fix shipped on feature/redesign-pass-1, VPS run pending) — new apps/medusa/src/scripts/fix-variant-inventory-flags.ts sets manage_inventory=false on every variant EXCEPT the group-session Live seats (idempotent, DRY_RUN by default, logs every change); creation paths patched to pass manage_inventory explicitly (migrate-woocommerce.ts: false everywhere except program_type "group" + "Live*" variants; seed.ts and seed-memberships.ts: always false); seed-group-session-inventory.ts already creates the stock location, links it to the default sales channel, and seeds the 12-seat levels — verified, no change needed there. To resolve: run on VPS inside the medusa container, in order: (1) DRY_RUN=false MEDUSA_ADMIN_PASSWORD=... npx ts-node src/scripts/fix-variant-inventory-flags.ts (2) MEDUSA_ADMIN_PASSWORD=... npx ts-node src/scripts/seed-group-session-inventory.ts, then complete a test checkout on a non-group-session product. |
| KI031 | Customer-facing pages and emails shipped fabricated testimonial content: TestimonialsSection.tsx (invented "James T."/"Priya K." quotes plus a literal unfilled "TODO: Real client name" card rendering in prod), TestimonialSpotlight.tsx (unverified "Sarah M." 2x-productivity story, also echoed in the /testimonials stat band), ProductPageContent.tsx (invented "Sarah M."/"James K." quotes on every product page), BookContent.tsx (invented "Michelle K."/"David T."/"Dr. Karen M." reader quotes), CartAbandonment3.tsx ("— Client Name, Location" placeholder quote), MembershipExpired.tsx ("— Member, Gold Tier" placeholder quote) | High | apps/web/components/home/, components/shop/, components/book/, components/testimonials/, lib/email/templates/ | 2026-08-04 | Resolved (2026-08-04, feature/redesign-pass-1) — all fabricated entries deleted; every testimonial slot now reads from the single typed source apps/web/data/testimonials.ts, which ships EMPTY by design. Each consuming section renders nothing (no heading, no layout hole) while its slot is empty. When Suzanne supplies verified quotes, paste them into data/testimonials.ts — nothing else needs editing. The /testimonials written grid and app/explore/topics.ts quotes were left in place (harvested verbatim from the old live site, not fabricated) but still deserve Suzanne's confirmation. Related open item: the /book pull-quote "excerpt" (BookContent.tsx ~line 397) is still placeholder book copy, TODO-marked, out of scope here. |
| KI029 | life-transitions-reinvention quiz (`apps/web/app/explore/quizzes/life-transitions.ts`) ships with 3 of its 4 results as placeholders. Only `fearful-starter` is sourced from a real completed ScoreApp run (screenshots in `Scoring App/vvbqjb24.scoreapp.com/`); `stalled-rebuilder`, `identity-drifter`, and `conscious-rebuilder` are drafted from the short landing-page archetype blurbs only, not full result pages. | Medium | apps/web/app/explore/quizzes/life-transitions.ts | 2026-07-29 | Resolved (2026-07-31) — `mirror`/`mechanism`/`impact`/`shift` rewritten for `stalled-rebuilder`, `identity-drifter`, and `conscious-rebuilder` to match the nervous-system.ts template depth (1-2 paragraph mirror, "Your system has learned: '...'" mechanism framing, 3-4 bullet impact/shift), expanded from the existing placeholder tone/theme rather than replaced outright. `fearful-starter` left untouched (already real sourced content). Top-of-file placeholder-status comment removed since all 4 results are now fully written. |
| KI032 | POPIA email compliance gaps: every marketing/transactional email template had href="#" unsubscribe links, footers lacked the physical business address, and the PDF tax invoice had no VAT registration number | High | apps/web/lib/email/, apps/web/emails/, apps/web/components/invoice/InvoiceDocument.tsx | 2026-08-04 | Partially resolved (2026-08-04, feature/redesign-pass-1) - code shipped: real HMAC-signed unsubscribe flow (lib/email/unsubscribe.ts + /unsubscribe page + POST /api/email/unsubscribe with RFC 8058 one-click headers), Supabase suppression table (supabase/migrations/20260804100000_email_unsubscribes.sql) honoured by the cart-abandonment and membership renewal/expired send routes, physical address + VAT number sourced from env with clearly-marked fallbacks. STILL OPEN, three deployment steps: (1) Suzanne to supply the real physical address and VAT number, then set COMPANY_PHYSICAL_ADDRESS + COMPANY_VAT_NUMBER in infra/.env on the VPS; (2) generate EMAIL_UNSUBSCRIBE_SECRET (openssl rand -hex 32) and add to infra/.env; (3) apply migration 20260804100000_email_unsubscribes.sql to Supabase. Until (1) is done, emails/invoices render a clearly-marked placeholder; until (2) is done, marketing sends fail loudly with "EMAIL_UNSUBSCRIBE_SECRET is not configured". |
| KI033 | Customer documents presented the company as VAT registered while Ravenall Institute is NOT VAT registered: the PDF invoice was a full SA "TAX INVOICE" (VAT Reg line, per-line "VAT 15%" column with 15/115 extraction, Subtotal excl VAT / VAT 15% / TOTAL incl VAT breakdown) and the order confirmation email showed a "VAT (15%)" row plus "VAT-compliant tax invoice" wording. A non-registered vendor may not issue tax invoices or show VAT amounts. | High | apps/web/components/invoice/InvoiceDocument.tsx, apps/web/emails/OrderConfirmation.tsx, apps/web/lib/email/order-confirmation.ts, apps/web/lib/email/company.ts | 2026-08-05 | Resolved (2026-08-05, feature/redesign-pass-1) - invoice + order confirmation email are now VAT-aware with VAT OFF as the default: companyVatNumber() returns null when COMPANY_VAT_NUMBER is unset, giving a plain "INVOICE" (PDF title too), "Reg No: 2012/180720/07" instead of the VAT Reg line, no VAT column/rows, tax-inclusive unit prices as-is, and a single TOTAL; order.tax_total is deliberately never rendered in this mode (tax_total > 0 while VAT-off = data inconsistency, see code comment). Setting COMPANY_VAT_NUMBER restores the full tax-invoice layout plus the Reg No line. Real address (8 Oxmoor Street, Kyalami Estates, Gauteng) and reg number hardcoded as public-identity defaults in company.ts, superseding KI032 deployment step (1); COMPANY_VAT_NUMBER must stay unset. OPEN DECISION for Johan: Medusa's SA region is seeded with a 15% "VAT" tax rate (apps/medusa/src/scripts/seed.ts:600 - though KI007 says the tax-rate seed FAILED on Medusa v2, so the live region may have no tax rate; verify on VPS) and the Sage integration books invoices against a 15% tax type (apps/medusa/src/modules/sage/service.ts:79). If checkout charges 15% tax the company cannot collect, the Medusa region tax rate and Sage tax type need changing - deliberately NOT changed here. |

---

## Template — Adding a New Issue

```
| KI{NNN} | {description} | {Critical/High/Medium/Low} | {file or location} | {month year} | Open |
```

## Resolving an Issue

Change status to `Resolved` and add the commit hash:

```
| KI001 | ... | ... | ... | ... | Resolved (abc1234) |
```
